<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.zhangpengpaul</groupId>
  <artifactId>shengwang-convoai-sdk-parent</artifactId>
  <version>0.1.0</version>
  <packaging>pom</packaging>
  <name>Shengwang ConvoAI Java SDK Parent</name>
  <description>Parent build for the Shengwang ConvoAI Java SDK and example modules.</description>
  <url>https://github.com/ZhangPengPaul/shengwang-convoai-sdk</url>

  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/licenses/MIT</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>ZhangPengPaul</id>
      <name>Peng Zhang</name>
      <url>https://github.com/ZhangPengPaul</url>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:git://github.com/ZhangPengPaul/shengwang-convoai-sdk.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/ZhangPengPaul/shengwang-convoai-sdk.git</developerConnection>
    <url>https://github.com/ZhangPengPaul/shengwang-convoai-sdk</url>
    <tag>HEAD</tag>
  </scm>

  <modules>
    <module>convoai-sdk</module>
    <module>convoai-sdk-examples</module>
  </modules>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <jackson.version>2.18.2</jackson.version>
    <junit.version>5.12.2</junit.version>
    <assertj.version>3.27.4</assertj.version>
    <mockwebserver.version>4.12.0</mockwebserver.version>
    <maven.compiler.plugin.version>3.14.0</maven.compiler.plugin.version>
    <maven.surefire.plugin.version>3.5.4</maven.surefire.plugin.version>
    <maven.source.plugin.version>3.3.1</maven.source.plugin.version>
    <maven.javadoc.plugin.version>3.11.2</maven.javadoc.plugin.version>
    <maven.gpg.plugin.version>3.2.7</maven.gpg.plugin.version>
    <central.publishing.plugin.version>0.9.0</central.publishing.plugin.version>
    <exec.maven.plugin.version>3.5.0</exec.maven.plugin.version>
    <maven.deploy.skip>true</maven.deploy.skip>
  </properties>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven.compiler.plugin.version}</version>
          <configuration>
            <release>${maven.compiler.release}</release>
          </configuration>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven.surefire.plugin.version}</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-source-plugin</artifactId>
          <version>${maven.source.plugin.version}</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>${maven.javadoc.plugin.version}</version>
        </plugin>

        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-gpg-plugin</artifactId>
          <version>${maven.gpg.plugin.version}</version>
        </plugin>

        <plugin>
          <groupId>org.sonatype.central</groupId>
          <artifactId>central-publishing-maven-plugin</artifactId>
          <version>${central.publishing.plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
